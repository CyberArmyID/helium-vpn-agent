version: "3.8"

services:
  vpn-agent:
    image: helium-vpn-agent:latest
    container_name: vpn-agent
    command: ["${UUID}"]
    restart: always
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
